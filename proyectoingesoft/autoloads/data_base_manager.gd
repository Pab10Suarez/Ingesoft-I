extends Node

# Éste script es un "autoload", el equivalente en Godot de los Singletons ( https://www.youtube.com/watch?v=09-aceKznw8 )

# Éste script debería tener las funciones a llamar para subir datos a la base de datos (como no hemos hecho todavía el formato de guardado de juego,
# puedes utilizar el tipado dinámico de GDScript para hacer que funcionen.

# Con F1 puedes acceder a la documentación desde el editor
func _ready() -> void:
	# Acá puedes testear el código, cuando le des "ejecutar", ésta función se llamará automáticamente (así la pantalla esté gris)
	# Un buen recurso para entender ésto es https://kidscancode.org/godot_recipes/3.x/basics/tree_ready_order/index.html
	pass
